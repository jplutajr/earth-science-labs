/* Public connection settings only. Never put a secret or service-role key here. */
window.CLASSROOM_CONFIG = Object.freeze({
  enabled: false,
  googleEnabled: false,
  supabaseUrl: "",
  publishableKey: ""
});
